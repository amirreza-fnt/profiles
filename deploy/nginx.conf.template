# Profile service listens directly on TCP 5027 (Kestrel).
# Domain apiweb-profilesystem.sabzevar.ir is already configured elsewhere (apis.conf).
# Point that existing SSL vhost upstream to: 127.0.0.1:5027
#
# Example location block to merge into the existing server { ... }:
#
#   location / {
#       proxy_pass http://127.0.0.1:5027;
#       proxy_http_version 1.1;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#   }
